#include <stdio.h>

int add(int a, int b){
    return a + b;
}

int main(void){
    /*
    the driver will mainly be here. i'll figure out this part last, because it's technically the easiset
    i already know that you uh... probably use the file input thing and pass each line to something...
    and use an if statement to check which function the file wants you to do...
    from there, set up the thing that checks and tests. that might have to be another method.
    passing things into *out will be your best friend. memory address or something can just be shared
    like a nice ice cream with your friend :>

    this is probably the hardest part actually. this will be worked on after convert.c is finished.
    */



    printf("i'm old!");
    return 0;
}